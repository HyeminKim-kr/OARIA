# Service Frontend: Mock Data → Real Papers API 연동

> **Category**: frontend
> **Date**: 2026-01-15
> **Status**: completed

---

## Context (배경)

- Service Frontend의 논문 목록이 현재 목데이터(mockPapers)로 표시됨
- Backend에 Papers API가 이미 구현되어 있음 (GET /papers/recent, GET /papers/search)
- 실제 수집된 논문 데이터로 교체 필요
- 기존 디자인은 반드시 유지

## Solution (해결 방안)

### 접근 방식
- React Query 설치 및 설정 (현재 미설치 상태)
- 기존 카드 UI를 PaperCard 컴포넌트로 분리
- API 연동 후 검색 기능도 함께 구현

### 작업 항목
- [x] React Query 설치
- [x] QueryProvider 생성 및 Layout 설정
- [x] API 타입 및 papersApi 추가
- [x] PaperCard 컴포넌트 생성
- [x] Main page 리팩토링 (mock → API)
- [x] 검색 기능 연동 (debounce 적용)

## Changes (변경 사항)

### 생성된 파일
- `frontend/src/components/providers/QueryProvider.tsx` - React Query Provider
- `frontend/src/components/papers/PaperCard.tsx` - Paper 카드 컴포넌트
- `frontend/src/components/papers/index.ts` - export

### 수정된 파일
- `frontend/package.json` - @tanstack/react-query 추가
- `frontend/src/app/layout.tsx` - QueryProvider 래핑
- `frontend/src/lib/api.ts` - Paper 타입 및 papersApi 추가
- `frontend/src/app/main/page.tsx` - mockPapers 제거, API 연동

## Notes

- likes 필드: 0으로 표시 (추후 좋아요 기능 추가 예정)
- 검색: debounce 300ms 적용

## Updates

| 시간 | 내용 |
|------|------|
| 15:00 | 초기 작성, 계획 수립 완료 |
| 15:30 | 구현 완료, 빌드 테스트 성공 |
