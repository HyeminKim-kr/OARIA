# Study Plan Evidence 탭 근거 목록 표시

> **Category**: frontend
> **Date**: 2026-01-29
> **Status**: completed

---

## Context (배경)

- 백엔드에서 `retrieved_papers`, `evidence_snippets` 배열을 정상적으로 전달
- 프론트엔드 `ResultsPanel`에서 숫자만 표시 (`paperCount`, `snippetCount`)
- evidence 탭에서 실제 논문/스니펫 목록 미표시
- "상세 근거 자료는 전체 계획서에서 확인할 수 있습니다" 안내 문구만 표시

---

## Solution (해결 방안)

### 작업 항목
- [x] `ResultsPanelProps`에 배열 타입 추가
- [x] `resultData` 상태에 배열 데이터 저장
- [x] evidence 탭 UI 구현 (논문 카드, 스니펫 카드)
- [x] 데이터 전달 코드 수정
- [x] 논문 카드 클릭 시 링크 이동 기능

---

## Changes (변경 사항)

### 수정된 파일

**`frontend/src/app/agents/study-plan/components/ResultsPanel.tsx`**
- `RetrievedPaper`, `EvidenceSnippet` 인터페이스 추가
- `ResultsPanelProps`에 `retrievedPapers`, `evidenceSnippets` 배열 prop 추가
- Evidence 탭 UI 구현:
  - 참고 논문 목록 (제목, 저널, 연도, 관련도)
  - 근거 스니펫 목록 (섹션, 관련도, 텍스트 미리보기)
  - 빈 상태 안내 메시지
- 논문 카드 클릭 시 링크 이동:
  - URL fallback 로직: `url` > `doi` > `pmcid` > `pmid`
  - 전체 카드를 `<a>` 태그로 감싸 클릭 가능하게 구현
  - URL 표시 및 hover 효과 추가

**`frontend/src/app/agents/study-plan/page.tsx`**
- `resultData` 상태에 `retrievedPapers`, `evidenceSnippets` 타입 추가
- `onCompleted`에서 `retrieved_papers`, `evidence_snippets` 저장
- `ResultsPanel`에 새 props 전달

---

## Updates

| 시간 | 내용 |
|------|------|
| - | 초기 작성 |
| - | 완료 |
