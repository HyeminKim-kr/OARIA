# Paper Chat 개선: 대화 저장 비활성화 + 요약 캐싱 시스템

> **Category**: backend
> **Date**: 2026-01-20
> **Status**: completed

---

## Context (배경)

Paper Chat에서 두 가지 개선 요구사항:
1. **대화 저장 비활성화**: Conversation, Message, AnswerLog 저장 안 함
2. **요약 캐싱 시스템**:
   - 첫 요약 요청 → DB 저장 + 백그라운드 임베딩
   - 이후 요약 요청 → DB에서 캐시된 요약 반환
   - 채팅 형식으로 스트리밍 응답

## Solution (해결 방안)
- [x] paper_summaries 테이블 설계 및 문서화
- [x] PaperSummary SQLAlchemy 모델 추가
- [x] Alembic 마이그레이션 생성 및 실행
- [x] paper_chat.py 대화 저장 로직 제거
- [x] paper_chat.py 요약 캐싱 로직 추가
- [x] Celery embed_summary 태스크 생성

## Changes (변경 사항)

### 생성된 파일
- `backend/alembic/versions/a5e42c02caf8_add_paper_summaries_table.py` - paper_summaries 마이그레이션
- `batch/src/tasks/embed_summary.py` - 요약 임베딩 Celery 태스크

### 수정된 파일 (Backend)
- `docs/database/papers.md` - paper_summaries 테이블 문서화
- `backend/app/models/paper.py` - PaperSummary 모델 추가, Paper에 summaries 관계 추가
- `backend/app/models/__init__.py` - PaperSummary export 추가
- `backend/app/routers/paper_chat.py`:
  - 대화 저장 로직 제거 (281-333 라인)
  - 요약 캐싱 로직 추가 (캐시 확인 → 캐시 히트/미스 처리)
  - `_trigger_embed_summary_task()` 헬퍼 함수 추가
- `batch/src/celery_app.py` - embed_summary 태스크 등록 및 라우팅 추가
- `batch/src/tasks/__init__.py` - embed_summary_task export 추가

### 수정된 파일 (Frontend)
- `frontend/src/components/paper-chat/hooks/usePaperChat.ts`:
  - `requestSummary()` 함수 추가 (SSE 스트리밍으로 채팅에 요약 표시)
  - `isSummary` 필드 추가하여 요약 메시지 구분
- `frontend/src/components/paper-chat/PaperChatPanel.tsx`:
  - 요약 버튼 클릭 시 모달 대신 `requestSummary()` 호출
  - 요약 관련 모달 코드 제거 (showSummaryModal, summaryLoading, summaryResult)

## Notes

### paper_summaries 테이블 스키마
```sql
CREATE TABLE paper_summaries (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    paper_id UUID NOT NULL REFERENCES papers(id) ON DELETE CASCADE,
    summary_type VARCHAR(20) NOT NULL DEFAULT 'full',
    summary TEXT NOT NULL,
    sections_used TEXT[],
    tokens_used INTEGER,
    embedding_status VARCHAR(20) DEFAULT 'pending',
    embedding_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ DEFAULT NOW(),
    updated_at TIMESTAMPTZ DEFAULT NOW(),
    UNIQUE(paper_id, summary_type)
);
```

### 요약 캐싱 흐름
1. `/summarize` 엔드포인트 호출
2. DB에서 기존 요약 캐시 확인
3. **캐시 히트**: 저장된 요약을 스트리밍 반환 (타이핑 효과)
4. **캐시 미스**: LLM으로 새 요약 생성 → DB 저장 → Celery 태스크로 백그라운드 임베딩 트리거

### 임베딩 저장 위치
- 기존 PaperChunk 컬렉션에 `section='summary'`로 저장
- RAG 검색 시 논문 요약도 검색 대상에 포함됨

## Updates
| 시간 | 내용 |
|------|------|
| 00:36 | 마이그레이션 파일 생성 |
| 00:40 | 마이그레이션 실행 완료 |
| 00:45 | paper_chat.py 대화 저장 로직 제거 |
| 00:50 | paper_chat.py 요약 캐싱 로직 추가 |
| 01:00 | Celery embed_summary 태스크 생성 및 등록 완료 |
