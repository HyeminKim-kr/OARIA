# Study Plan Agent - 페이지 새로고침 시 상태 유지

> **Category**: frontend
> **Date**: 2026-01-27
> **Status**: completed

---

## Context (배경)
- Study Plan Agent 페이지에서 실험 계획 생성 중 새로고침하면 landing 페이지로 돌아감
- `viewMode` state가 React state로만 관리되어 새로고침 시 초기값 "landing"으로 리셋됨
- 진행 중인 작업 상태도 함께 유실됨

## Solution (해결 방안)
- [x] Task 1: sessionStorage를 사용하여 viewMode 상태 유지
- [x] Task 2: 진행 중인 jobId가 있으면 자동으로 generate view로 전환
- [x] Task 3: 작업 완료/리셋 시 sessionStorage 정리
- [x] Task 4: Hydration 이슈 방지를 위한 로딩 상태 추가

## Changes (변경 사항)

### 수정된 파일
- `frontend/src/app/agents/study-plan/page.tsx`
  - `STORAGE_KEY`, `STORAGE_JOB_KEY` 상수 추가
  - `isHydrated` state 추가 (SSR/CSR 불일치 방지)
  - `updateViewMode` 콜백 함수 추가 (sessionStorage 동기화)
  - 마운트 시 저장된 viewMode/jobId 복원하는 useEffect 추가
  - jobId 변경 시 sessionStorage에 저장하는 useEffect 추가
  - 뒤로가기 시 모든 상태 초기화 (thinkingSteps, resultData 등)
  - Hydration 완료 전 로딩 스피너 표시

## Notes
- sessionStorage 사용으로 탭 단위로 상태 유지 (브라우저 닫으면 초기화)
- localStorage 대신 sessionStorage 선택 - 탭별 독립 상태 관리
- Hydration 완료 전에는 로딩 스피너 표시하여 깜빡임 방지

## Updates
| 시간 | 내용 |
|------|------|
| 14:00 | 초기 작성, 문제 분석 완료 |
| 14:15 | 구현 완료 |
