# ConfigModule 환경변수 검증 추가

> **Category**: admin
> **Date**: 2025-12-31
> **Status**: completed

---

## Context (배경)
- 프로덕션 환경에서 환경변수 누락 시 런타임 에러 발생 가능
- 로컬(development)에서는 디폴트값으로 편하게 개발 가능해야 함
- 프로덕션/스테이징에서는 모든 필수 환경변수가 명시되어야 함

## Solution (해결 방안)
- [x] class-validator를 활용한 환경변수 검증 스키마 작성
- [x] LOCAL_DEFAULTS 객체로 디폴트 값 중앙 관리
- [x] @IsOptional 사용 안함 - 모든 필드 필수
- [x] ConfigModule.forRoot에 validate 함수 연결
- [x] .env.example 업데이트

## Changes (변경 사항)

### 생성된 파일
- `src/config/env.validation.ts` - 환경변수 검증 스키마 및 validate 함수
- `src/config/index.ts` - config 모듈 export

### 수정된 파일
- `src/app.module.ts` - ConfigModule에 validate 함수 연결
- `.env.example` - 전체 환경변수 목록 및 설명 추가

## Notes
- **@IsOptional 사용 안함** - 모든 필드 필수 검증
- **LOCAL_DEFAULTS**: 로컬 환경 디폴트 값 중앙 관리
- **development/test**: LOCAL_DEFAULTS 적용 후 검증
- **production/staging**: 디폴트 없이 검증 (누락 시 서버 시작 실패)

## 환경변수 목록

| 카테고리 | 변수명 |
|----------|--------|
| App | NODE_ENV, PORT |
| Database | DB_HOST, DB_PORT, DB_USER, DB_PASSWORD, DB_NAME |
| Redis | REDIS_HOST, REDIS_PORT |
| S3/MinIO | S3_ENDPOINT, S3_ACCESS_KEY, S3_SECRET_KEY, S3_BUCKET |
| JWT | JWT_SECRET, JWT_ACCESS_EXPIRES, JWT_REFRESH_EXPIRES_DAYS |
| Google OAuth | GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, GOOGLE_CALLBACK_URL |
| Super Admin | SUPER_ADMIN_EMAIL |
| Frontend | ADMIN_FRONTEND_URL |
| Weaviate | WEAVIATE_HOST, WEAVIATE_PORT |
| Flower | FLOWER_HOST, FLOWER_PORT, FLOWER_USER, FLOWER_PASSWORD |

## Updates
| 시간 | 내용 |
|------|------|
| 00:10 | 히스토리 파일 생성 |
| 00:15 | 환경변수 검증 스키마 작성 완료 |
| 00:20 | 빌드 검증 완료 |
| 00:35 | 리팩토링 - @IsOptional 제거, LOCAL_DEFAULTS 중앙 관리 |
